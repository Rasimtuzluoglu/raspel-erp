global:
  resolve_timeout: 5m

route:
  receiver: 'default'
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 4h

receivers:
  - name: 'default'
    # Slack/Email/Telegram entegrasyonu icin buraya webhook ekleyin:
    # slack_configs:
    #   - api_url: '${SLACK_WEBHOOK_URL}'
    #     channel: '#alerts'
    # email_configs:
    #   - to: 'admin@raspel.com'
    #     from: 'alerts@raspel.com'
    #     smarthost: 'smtp.gmail.com:587'
